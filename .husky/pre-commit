#!/usr/bin/env sh

# Report remaining advisories; do not block on unfixable transitive deps
bun run security:fix || echo "warning: bun audit fix reported remaining issues"

# Auto-fix formatting issues (CSS, Markdown, Prettier)
bun run format
